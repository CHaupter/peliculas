import 'package:flutter/material.dart';

class MovieSearchDelegate extends StatelessWidget {
  const MovieSearchDelegate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
